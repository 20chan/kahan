@page "/"
@namespace kahan.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>kahan</title>
    <base href="~/"/>
    <link rel="stylesheet" href="css/bootstrap/bootstrap.min.css"/>
    <link href="css/site.css" rel="stylesheet"/>
</head>
<body>
<app>
    <component type="typeof(App)" render-mode="ServerPrerendered"/>
</app>

<div id="blazor-error-ui">
    <environment include="Staging,Production">
        An error has occurred. This application may no longer respond until reloaded.
    </environment>
    <environment include="Development">
        An unhandled exception has occurred. See browser dev tools for details.
    </environment>
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

<div id="music-player" style="display:none"></div>

<script src="_framework/blazor.server.js"></script>
<script src = "https://www.youtube.com/iframe_api"></script>
<script>
function onYouTubeIframeAPIReady() {
    window.player = new YT.Player('music-player', {
        videoId: 'p1asWglxMZs',
        events: {
            'onReady': onPlayerReady,
            'onStateChange': onPlayerStateChange
    }});
}
function onPlayerReady(event) {
    // event.target.playVideo();
}
function onPlayerStateChange(event) {
}
function getPlayerCurrentTime() {
    if (window.player.getCurrentTime === undefined) {
        return 0;
    }
    return window.player.getCurrentTime();
}
function getPlayerDuration() {
    if (window.player.getCurrentTime === undefined) {
        return 0;
    }
    return window.player.getDuration();
}
function loadVideo(id) {
    window.player.loadVideoById(id, 0, "large");
}
function addTime(time) {
    window.player.seekTo(player.getCurrentTime() + time, true);
}
</script>
</body>
</html>